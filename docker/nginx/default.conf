server {
    listen 80;
    listen [::]:80;
    server_name _;

    location / {
        proxy_pass http://result:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        alias /static/;
    }
}
